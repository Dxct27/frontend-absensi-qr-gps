DONE: 
yahoo oauth 
excel export 
set user password after oauth login
userleavepermissionmodal
QRFormModal
crud qr modalll
qr time format -- utc jakarta mismatch try yourself

fix attendance status. no such thing terlambat, H, I, S, A, DL, log time stamp and loc
(add column ? lokasi di luar radius?) ga soale lek ga ada di tempat tidak bisa dibilang "hadir"

MIGRATION ATTENDANCE TABLE ADD COLUMN (cancel, it was qrcodes (types and event_id), add special_events table)

admin QRcodepage -- fetch and show all active qr
new page for qr history

TODO:

!!! qrcode list show all attendance and qrcodecomponent
handle view leave request file.

fix styling 

MODALLL

auto create?

handle file upload done --view file belom

docs

User Dashboard --when already scan and waiting for api req add loading munyer? UI/UX

no such thing as leave permission on daily shit on weekend

handle lokasi --wajib "pastikan lokasi Anda sesuai/tepat/something else"

qrpage admin show current active qr and add history (page?) for previous

admin detail can see where barcode scanned (?)

qrhistorypageee --list
in this page, add filter for daily and event

event --dinas penyelenggara, peserta dari dinas mana.

user history add for event (list saja buat event).

SCHEDULER AND AUTOMATION, FETCH API SPLP, QR DAILY AUTO CREATE, DOCS